import React from 'react';
import ReactDOM from 'react-dom';
import {useStrict} from 'mobx';

// Mobx strict mode
useStrict(true);

// App rendering, injecting into real dom.
// ReactDOM.render(Component, document.getElementById('app'));
